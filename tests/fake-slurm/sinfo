#!/usr/bin/env bash
# Fake sinfo: prints $FAKE_SLURM_DIR/sinfo verbatim (nothing when absent),
# whatever the arguments. See README.md.

# shellcheck source=tests/fake-slurm/lib.sh
source "$(dirname "$(readlink -f "$0")")/lib.sh"
log_call sinfo "$@"
cat_fixture sinfo
