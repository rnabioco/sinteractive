#!/usr/bin/env bash
# Fake sacctmgr: prints $FAKE_SLURM_DIR/qos verbatim (nothing when absent),
# whatever the arguments. See README.md.

# shellcheck source=tests/fake-slurm/lib.sh
source "$(dirname "$(readlink -f "$0")")/lib.sh"
log_call sacctmgr "$@"
cat_fixture qos
