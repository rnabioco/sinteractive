//! `sinteractive [launch]` — TODO(phase-1/agent-C).

use anyhow::Result;

use crate::cli::LaunchArgs;

pub fn run(_args: LaunchArgs) -> Result<i32> {
    anyhow::bail!("launch is not implemented yet")
}
