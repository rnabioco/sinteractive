// The sinteractive session layout: a shell above the one-row status bar.
// `__PLUGIN__` is substituted with the absolute path of sint-zellij.wasm.
// `sint-panel.kdl` is the same with the monitor panel; the bar plugin swaps
// between the two with override_layout on Ctrl+b m.
layout {
    pane borderless=true focus=true
    pane size=1 borderless=true {
        plugin location="file:__PLUGIN__" {
            view "bar"
        }
    }
}
